/**
 * Stub for DemoTrackingService
 * Backend services removed for frontend-only build
 */

export class DemoTrackingService {
  track() {
    console.warn('[DemoTrackingService] Backend service disabled - frontend-only mode');
  }
}

