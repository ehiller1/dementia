/**
 * Stub for ReactiveTemplateUpdater
 * Backend services removed for frontend-only build
 */

export function initializeReactiveTemplates() {
  console.warn('[ReactiveTemplateUpdater] Backend service disabled - frontend-only mode');
}

