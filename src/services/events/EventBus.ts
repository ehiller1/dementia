/**
 * EventBus removed - stub to prevent import errors
 */

export const eventBus = {
  publish: () => {},
  subscribe: () => ({ unsubscribe: () => {} }),
  on: () => {},
  off: () => {},
  emit: () => {},
  once: () => {},
} as any;

